# Add replication dependencies here.
# Pin the KnowledgeVault svar-python package before final sharing, for example:
# svar-python @ git+https://github.com/<owner>/KnowledgeVault.git@<commit>#subdirectory=svar-python
