# Tables

Store manuscript tables here when they are part of the paper.

Every final table should have a corresponding entry in
`../formal-object-registry.json` and a reproducible source under
`../replication/` when possible.
